UML Çalışma — Slayt odaklı
==========================

Semboller
---------
  - alan : Tip       private
  + metot()          public
  # alan             protected
  {static}           static
  {abstract}         abstract

Kalıtım (extends):     Alt ──▷ Ust
Interface (implements): Sinif ─ ─ ▷ IF


1) VİZE — Book (vize_ornekler/Book.java, Slayt 3/8)
---------------------------------------------------

┌─────────────────────────────────────────┐
│                  Book                   │
├─────────────────────────────────────────┤
│ - title: String                         │
│ - author: String                        │
│ - price: double                         │
│ - bookCount: int {static}               │
├─────────────────────────────────────────┤
│ + Book()                                │
│ + Book(title: String, author: String)   │
│ + Book(title, author, price: double)    │
│ + displayBook(): void                   │
│ + getPrice(): double                    │
│ + applyDiscount(percentage: double)     │
│ + getBookCount(): int {static}          │
└─────────────────────────────────────────┘


2) FİNAL — Polimorfizm (Slayt 10, ch10 örneği)
----------------------------------------------

        Shape {abstract}
           ├── Circle
           └── Rectangle

  {abstract} + area(): double


3) FİNAL — Generic + Collections (Slayt 16/20)
----------------------------------------------

  Box<T>
    - value: T
    + get(): T

  Student implements Comparable<Student>
    + compareTo(other): int


4) KISA SORULAR
---------------
extends vs implements ok farkı?
abstract sınıftan nesne oluşturulur mu? → Hayır
private constructor ne işe yarar? → static factory; new X() yasak
